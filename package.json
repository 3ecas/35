{
  "name": "thirtyfive",
  "version": "1.0.0",
  "private": true,
  "description": "35 — a drop-and-merge number puzzle. Static web app, wrapped for iOS with Capacitor.",
  "scripts": {
    "serve": "python3 .claude/serve.py 4173",
    "build": "rm -rf www && mkdir -p www && cp index.html www/ && cp -R css js www/ && node tools/stamp.js",
    "icons": "swift tools/icon.swift",
    "palette": "node tools/palette.js --write",
    "ios": "npm run sync && LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 DEVELOPER_DIR=\"${DEVELOPER_DIR:-/Applications/Xcode.app/Contents/Developer}\" npx cap open ios",
    "sync": "npm run build && LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8 DEVELOPER_DIR=\"${DEVELOPER_DIR:-/Applications/Xcode.app/Contents/Developer}\" npx cap sync ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.8"
  },
  "dependencies": {
    "@capacitor/core": "^7.6.8",
    "@capacitor/ios": "^7.6.8"
  }
}
